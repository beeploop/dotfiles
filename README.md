# Dotfiles
Personal development environment configuration.

## Usage
For the mean time, my preferred approach is manually copying the configs to the necessary directories by creating `symbolic links`. For each configuration, the folder structure describes where the configuration file(s) should be placed.
